# Local-first document processing

Create a 12-slide, 16:9 technical explainer for software engineers. Explain a local pipeline that accepts an Office file, validates the package, builds a normalized document model, runs deterministic checks, renders previews, writes an offline report, and returns a process exit code.

Cover architecture, trust boundaries, input limits, data flow, failure handling, privacy, testing, deployment, operational trade-offs, and next steps. Do not claim that the system checks factual truth or visual taste. Deliver an editable `.pptx` where practical.
